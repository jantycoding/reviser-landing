/**
 * Знак бренда — клодик в минималистичном стиле, серый.
 *
 * Решение владельца 14.08.2026, референс — шапка pleep (маленький глиф +
 * плотный словесный знак). Отменяет пятиугольник «Metal Noir» от 06.08.2026.
 *
 * Силуэт — тот же контур, что у маскота в Claudik.jsx (тело, без ножек-
 * деталей). Глаза вырезаны ЦВЕТОМ ФОНА через var(--color-ink): на белой
 * странице вырез белый, внутри тёмной зоны героя (scope-dark) — чёрный.
 * Перекрашивать знак под контекст не нужно, токен делает это сам.
 *
 * Цвет — var(--color-chalk): чёрный на белой странице (запрос владельца
 * 14.08: «серый делаю чёрным»), белый внутри тёмной зоны героя. Адаптивно.
 * На 20px (вкладка) глаза схлопываются в две точки, силуэт остаётся
 * узнаваемым.
 */
export default function Logo({ className = 'h-8 w-8' }) {
  return (
    <svg viewBox="0 0 48 44" className={`${className} shrink-0`} aria-hidden="true" focusable="false">
      <path d="M8 4H40V12H46V26H40V40H33V31H28V40H20V31H15V40H8V26H2V12H8Z" fill="var(--color-chalk)" />
      <rect x="15" y="14" width="5.5" height="5.5" fill="var(--color-ink)" />
      <rect x="27.5" y="14" width="5.5" height="5.5" fill="var(--color-ink)" />
    </svg>
  );
}
