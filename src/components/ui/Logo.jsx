/**
 * Знак бренда — минималистичная метка вместо маскота (решение владельца,
 * 06.08.2026, переоформлена под палитру «Metal Noir» тем же днём). Прежний
 * клодик-мурал хорошо работает в демо отчёта (там он остаётся в
 * `agents/Claudik.jsx`), но для шапки и фавикона — знак, который читается
 * за 20px: клодик, свёрнутый в неровный пятиугольник, залитый
 * серебристо-металлическим градиентом, без деталей лица.
 *
 * Знак нарисован inline-SVG, а не файлом в `public`: одним запросом меньше и
 * нечему ломаться, если каталог переедет.
 */
export default function Logo({ className = 'h-8 w-8' }) {
  return (
    <svg viewBox="0 0 32 32" className={`${className} shrink-0`} aria-hidden="true" focusable="false">
      <defs>
        <linearGradient id="rv-logo-bg" x1="0" y1="0" x2="1" y2="1">
          {/* Metal Noir: серебро → белый → холодный металл, без цвета. */}
          <stop offset="0%" stopColor="#e4e7ec" />
          <stop offset="45%" stopColor="#b9bec7" />
          <stop offset="100%" stopColor="#7d828c" />
        </linearGradient>
      </defs>
      {/* Неровный пятиугольник — «клодик», свёрнутый в геометрическую метку:
          пять точек с намеренно смещёнными углами, не правильная фигура. */}
      <path d="M16 3.5L28 12l-3.6 15H7.6L4 12z" fill="url(#rv-logo-bg)" />
      {/* Тонкая внутренняя грань — намёк на лицо клодика, без деталей. */}
      <path
        d="M16 9.5l6.2 7.4-2.4 6.6h-7.6l-2.4-6.6z"
        fill="none"
        stroke="#000000"
        strokeWidth="1.1"
        strokeLinejoin="round"
        opacity="0.35"
      />
    </svg>
  );
}
